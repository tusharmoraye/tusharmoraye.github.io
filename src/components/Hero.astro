---
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";
import DitherTrail from "./DitherTrail.astro";
import portraitImg from "../assets/hero/tushar_portrait.png";
---

<section id="top" class="relative min-h-[100dvh] overflow-hidden pt-24 pb-16">
  <div class="rule-grid pointer-events-none absolute inset-0 opacity-70" aria-hidden="true"></div>
  <div
    class="pointer-events-none absolute -top-[20%] left-1/2 h-[70vh] w-[110vw] -translate-x-1/2 rounded-full blur-[120px]"
    style="background: radial-gradient(closest-side, var(--glow), transparent);"
    aria-hidden="true"
  >
  </div>

  <DitherTrail />

  <div class="shell relative z-[2] grid min-h-[calc(100dvh-10rem)] items-center gap-12 lg:grid-cols-12 lg:gap-8">
    <div id="hero-copy" class="lg:col-span-7 xl:col-span-6">
      <h1
        class="font-display text-[clamp(2.2rem,4.4vw,3.5rem)] font-semibold leading-[1.02] tracking-[-0.035em]"
      >
        <span class="line-mask" style="--line-delay:80ms"><span>Conversational AI,</span></span>
        <span class="line-mask" style="--line-delay:200ms"
          ><span class="text-accent">shipped to production.</span></span
        >
      </h1>

      <p
        class="reveal mt-7 max-w-[46ch] text-base leading-relaxed text-muted md:text-lg"
        style="--reveal-delay:520ms"
      >
        Eight years building product surfaces for AI and ML systems. Currently architecting the
        Copilot interface at Atlan.
      </p>

      <div class="reveal mt-10 flex flex-wrap items-center gap-3" style="--reveal-delay:640ms">
        <a href="#contact" class="btn btn-primary">
          Get in touch
          <Icon name="ph:arrow-up-right-bold" class="size-4" />
        </a>
        <a href="#work" class="btn btn-ghost">See the work</a>
      </div>
    </div>

    <div class="lg:col-span-5 lg:col-start-8 xl:col-start-9">
      <div class="relative max-w-[440px] mx-auto lg:max-w-none">
        <!-- Ambient radial glow behind portrait -->
        <div
          class="pointer-events-none absolute -inset-2 rounded-2xl bg-accent/15 blur-2xl [html[data-theme='light']_&]:bg-accent/10"
          aria-hidden="true"
        ></div>

        <figure
          class="reveal relative aspect-[3/4] w-full overflow-hidden rounded-xl border border-line/90 bg-surface-2 shadow-2xl transition-transform duration-500 hover:scale-[1.01]"
          style="--reveal-delay:360ms"
        >
          <Image
            id="hero-plate"
            src={portraitImg}
            alt="Tushar Moraye - AI Product Engineer"
            width={1086}
            height={1448}
            fetchpriority="high"
            loading="eager"
            decoding="async"
            class="plate size-full object-cover"
          />

          <!-- Floating badge at bottom of portrait -->
          <div class="pointer-events-none absolute inset-x-3 bottom-3 z-10 flex items-center justify-between rounded-lg border border-line/80 bg-bg/85 px-3 py-2 backdrop-blur-md shadow-lg">
            <div>
              <p class="font-sans text-xs font-semibold text-ink">Tushar Moraye</p>
              <p class="font-mono text-[10px] text-muted">Senior AI Engineer @ Atlan</p>
            </div>
            <div class="flex items-center gap-1.5 font-mono text-[10px] text-accent">
              <span class="size-1.5 rounded-full bg-accent animate-ping"></span>
              <span>Mumbai / Remote</span>
            </div>
          </div>
        </figure>
      </div>
    </div>
  </div>
</section>

<script>
  const copy = document.getElementById("hero-copy");
  requestAnimationFrame(() => copy?.classList.add("hero-ready"));
</script>
